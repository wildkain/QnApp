- if user_signed_in?
  =< link_to t('links.navbar.logout'), destroy_user_session_path, method: :delete, class: 'nav-link'
- else
  =< link_to t('links.navbar.login'), new_user_session_path, class: 'nav-link'